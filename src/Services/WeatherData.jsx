// The below code exports a variable named weather_data which is initialized with null. This variable is created using the createContext function from the React library. The createContext function creates a new context object that can be used to share data between components in a React application.

import { createContext } from "react";

const weather_data = createContext([]);

export default weather_data;
